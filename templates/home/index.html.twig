<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fake form</title>
    <link href="{{asset('assets/css/home.css')}}" rel="stylesheet">
</head>
<body>

  {# poster="{{ asset('assets/videos/fond.png') }}" #}
<video playsinline autoplay muted loop>
    <source src="{{ asset('assets/videos/video_1.mp4') }}" type="video/mp4">
</video>
<!--
- 9
- 6
- 4
- 7
 -8
-->

    <header>
    <h1>The fundamentals</h1>
      <nav>
        {% if app.user %}
                    <a href="{{ path('app_account') }}">Mon compte <small>({{app.user.firstname}})</small></a> | <a href="{{ path('app_logout') }}">Deconnexion</a> |
        {% else %}
            <a href="{{ path('app_login') }}">Connexion</a> | <a href="{{ path('app_register') }}">Inscription</a> |
        {% endif %}
        <a href="#">Contact</a>
    </header>
    <div id="fashion">
        <a href="{{ path('app_products') }}" class="btn-link">
            <h2>Products</h2>
          </a>          
    </div>

  </body>
  </html>